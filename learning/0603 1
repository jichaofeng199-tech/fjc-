颜色校正（Color Correction，CC）最大程度减少偏色以及提高颜色保真
度。使场景中的色彩经过光学器件、传感器输出到显示设备上时，能够提供适
应人眼色彩感知的颜色复原。
CCM 矩阵通过调试相机像素矩阵与目标相机像素矩阵最小二乘法计算得
出：
[
��𝑡𝑎��
��𝑡𝑎𝑟
��𝑡𝑎𝑟] = [
�11 �12 �13
�21 �22 �23
�31 �32 �33] [
��𝑐𝑎𝑚
��𝑐𝑎𝑚
��𝑐𝑎𝑚]
为了保证白点还是白点，加入约束条件：
�11 + �12 + �13 = 1
�21 + �22 + �23 = 1
�31 + �32 + �33 = 1
假设调试相机为矩阵 A，目标相机为矩阵 B，使用拉格朗日乘数法求解得
M 矩阵：
[
�11 �12 �1
�21 �22 �2
�31 �32 �3][
�11 − �31 �12 − �32 �13 − �33
�21 − �31 �22 − �32 �23 − �33
−1
−1 −1 ] = [
�11 − �31 �12 − �32 �13 − �33
�21 − �31 �22 − �32 �23 − �33
�31 − �31 �32 − �32 �33 − �33]
其中，目标相机矩阵是 CIE1931 标准观察者空间得到的 RGB 值，在指定
光源下计算出像素值，其值在 XYZ 空间且未进行 AWB，因此通过 Branford 或
者 CAT02 进行色适应计算，然后再转换到 sRGB 色域或 P3 色域。
调试相机像素矩阵利用相机响应曲线或实际拍图获得，前者同目标相机处
理流程，后者在对应光源下对色卡进行拍照，测出环境光源曲线。
2.5.2 调节颜色校正矩阵
调节 CCM 前，伽马曲线必须准确，任何一次伽马曲线的调整在参数编辑
器调节 CC 矩阵的触发器包括色温（D65、D50、TL84、A、H）、亮度 AEC
（弱光和室外矩阵）、LED flash。在 3×3 矩阵中，对角线上最大值影响因子越
大，值越高表示更高的色彩饱和度和更大的噪声，因此对角线值的理想范围是
[1.8,3]，除对角线以外的值根据色彩融合示意图进行偏色调整。
2.5.3 调试示例
问题场景：三供三方拍照红色偏紫。
先确定白平衡不存在偏色问题：
随后抓 log 和拍摄此场景时的 metadata 信息，确定参数修改路径和问题场景色温和亮度水平。
根据色彩融合图，确定该场景存在红色饱和度不够和 rg 偏小的问题。因此在调节 3×3 CCM 矩阵时，增大相应亮度和色温区间的 rr 和 rg 的值：
最终修改结果如下：
2.6 2DLut 模块
2.6.1 2DLut 原理
查找表是图像颜色转换的工具，将一组 RGB 值输出为另一组 RGB 值，从而改变画面的曝光与色彩。
LUT（src(I) + d） → dst（I）
1Dlut 只影响 gamma，2Dlut 在三通道之间的处理是互相独立，3Dlut 对每一个坐标方向都有 RGB 通道，能够同时影响色相、饱和度和亮度，并可以映射处理所有色彩。
2.6.2 调节 2DLut
 在 main 界面上选择要调整的图像，在色相图上拖动关键点至所需的颜色，
数值的变化体现在 Hue 和 Saturation 页面上，在调整过程中，相邻关键点也要适当调整使其更改更加平滑。
三、AI 平台调试模块原理及其应用
3.1 AIAWB
3.1.1 AIAWB 算法原理
整个算法包括迁移矩阵转换、AIAWB 网络和 Cast Merge Finetune（CMF）算法三部分，调试手机落点统计信息通过迁移矩阵转换为目标手机落点位置，再通过 AIAWB 网络对落点统计信息进行语义提取和特征提取，输出 AI 白点结果，经过 CMF 算法处理与 QAWB 结果融合，生成最终决策值。算法流程如下图所示。
三个模块的具体操作如下所示：
1) 转换迁移矩阵前置使用 RAW 域，后置使用 XYZ 域，该矩阵将 20 张目标
手机与调试手机的光源图像 RGB 值拟合训练生成，通过矩阵将调试手机的落点位置迁移到目标手机的落点位置。
2) AIAWB 网络通过输入预览帧的 raw 图及辅助统计信息，基于卷积神经网
络计算得到当前帧的语义信息（Semantic Information Extraction Using Depth Wise Convolusion，S）和颜色信息（Color Correlation Extraction Using Depth Wise Convolusion，C），S 和 C 提取的信息并不完全独立，加入两个 branch 进行参数共享，解决过拟合问题；加入通道加权池
（Channel Wise Weighted Pooling）将语义信息嵌入到颜色信息中，提高
颜色预测准确性，输出灰点值 RG,BG。
3) CMF 算法从色温和色度进行决策点优化，分为以下步骤：
i. 双光源融合（Duo Illum）对高色温白点与低色温白点进行加权融合，每
个数值表示高低色温融合的权重，即最终白点 = n × 低色温白点 + （1 −n） × 高色温白点。
ii. 色度生成（Cast Duv）调整 AIAWB 红绿倾向。表格中的数据表示落点偏
离普朗克轨迹线的距离，距离正（负）值越大表示偏绿（红）越严重。iii. 色温生成（Cast CCT）调整 AIAWB 冷暖倾向，正（负）值越大表示偏暖
（冷）越严重。
iv. 融合色度（Merge Duv）调整融合 QAWB 和 AIAWB 白点信息的色度权
重，数值越大，QAWB 融合权重越大。
v. 融合色温（Merge CCT）调整融合 QAWB 和 AIAWB 白点信息的色温权
重，数值越大，QAWB 融合权重越大。
(a) (b)  (c)
 (d) (e)
图 从(a)-(e)依次为 Duo Illum、Cast Duv、Cast CCT、Merge Duv 以及 Merge CCT 表格数据
作用 名称 定义
调试策略
CastDuv 调整AIAWB的红绿 调整相应色温和亮度值的相邻四个值区间的大小（红绿）值
MergeDuv
调整AI与Hisi点融合后的红
绿
1. 判断hisi点比ai红还是绿；
2. hisi点比ai红（绿），ai向红（绿）调整，增大上（下）表格相应相邻区间的
数值；
3. 调整区间为【30,70】。
CastCCT 调整AIAWB的冷暖 调整相应色温和亮度值的相邻四个值区间的正负（暖冷）值
MergeCCT 调整AI与Hisi点融合后的冷暖
4. 判断hisi点比ai冷还是暖；
5. hisi点比ai暖（冷），ai向暖（冷）调整，增大左（右）表格相应相邻区间的
数值；
6. 调整区间为【30,70】。
网络参数调整：观察最终 cmf 落点与 demo 点的差距来进行参考调整，使落
点向 demo 点靠拢。
名称
概念
hisi
QAWB结果
Ai&Ex AIAWB双光源AWB结果
mergeillum Ai和Ex两者结果加权生成
castTemp mergeillum经过castTemp表处理生成
cmf
castTemp与QAWB进行cmf算法加权融合
Log 分析：分为 AIAWB 结果、对外发布结果以及接受结果（屏显结果）
3.1.2 调试示例
问题场景：室外黑天偏蓝
将后置拍摄图片拉入软件 AIAWB_QcomPhotoPlot_back，该软件主要对图片的色温（偏暖黄/冷蓝）和色调（偏红/绿）进行手动微调。导入图片后生成图片维测信息，如下图所示。
根据维测信息可看出，AIAWB 的落点较 QAWBD b/g 值更小，即其更偏向于蓝色高色温权重，因此造成蓝色溢出，利用 CASTDuv 表调整 AIAWB
的落点倾向，使其最终落点向 B/G 增大的方向移动。
3.2 动态颜色校正
3.2.1 动态颜色校正算法原理
动态颜色校正（Color Correction，CC）整体处理流程如下：同一场景中的 AWB 白点经过环境光谱、24 色卡反射率谱，再分别经过标准观察者响应曲线和相机光谱响应曲线生成各自的三刺激值，两者进行最小二乘拟合输出cam2std，算出颜色校正矩阵，随后在空间进行色适应，在 LAB 空间进行保色度，生成最终 CCM。
图 动态 cc 算法流程，注：Commission Internatioonale Declairage （CIE）国际照明委员会；Color Adaptation Mat（CA Mat）色适应映射表；Sample Rate 
Converter(SRC)，采样率转换器； Dynamic Storage Tiering(DST)，动态存储分层。 
色适应映射表：色适应 CA 算法根据 gain 值查找对应的 cam2std_awb 矩阵转换至标准观察者 XYZ 空间，计算当前环境的色温色度，根据白点计算人眼的色适应状态，补偿图像的光源倾向。
根据 AWB gain 值查找对应的 cam2std_awb 矩阵，将 raw 转换到标准观察者 XYZ空间，XYZ 空间白点通过 Cat02 色貌矩阵转换到 LMS 空间；通过 CCT、DUV、亮度在配置的色适应查找表中获取色适应程度 D；在 LMS 空间将 SRC 白点往 D65 白点方向做色适应程度为 D 变换 ，计算出色适应矩阵 CA_Mat。
保色度：从 XYZ 域转到 LAB 空间，亮度和饱和度归一，只留下 hue 通道做最小二乘，生成 Hue Prot，该操作将三原色向目标值靠拢，不保色度的话颜色偏移不稳定有交叉，lut 不好拉。用合成的白点左乘 P3_2_XYZ 转换至 XYZ 域，再左乘
caMat 的倒数，得到 Hue Prot 的 cam2std_ccm，再乘以色适应值 caMat 转换至
cam2P3。
3.2.2 调节动态颜色校正
调试策略：修改文档 CorrectsatBylvCam2std.h 的 targetXYZ 调节目标值饱和
度；修改文档 CorrectsatBylvDivCcm.h 的 CCM 调节整体图像的饱和度；
文档
修改内容
调试方法
CorrectsatBylvCam2std.h targetXYZ的饱和度
⚫ 第1列为亮度区间；
⚫ 第2列为24色卡对应亮度下的饱和度调节比；
⚫ 值越大，饱和度越高。
CorrectsatBylvDivCcm.h 整体图像的饱和度
⚫ 第1列为亮度区间；
⚫ 第2列为对应亮度的饱和度调节比；
⚫ 值越大，饱和度越高。
ccmCorrectCoef.h
标准相机下24色卡的饱和度、在
D光下和A光下的色相
⚫ Sat Ratio值越大，该色号饱和度越高；
⚫ CamCoeffR& CamCoeffB：表示24色卡在D光下的Rgain&Bgain，
值越大，越偏绿/红；
⚫ CamCoeffRa& CamCoeffBa：表示24色卡在A光下的
Rgain&Bgain，值越大，越偏绿/红。
3.2.3 高维 cc 原理
相较于动态 cc 使用的线性颜色矩阵，高维 cc 使用根多项式颜色矩阵（Root-
Polynomial Colour Transform），在原始的 3×3 矩阵变为 3×6 矩阵，加入非线性
拓展项√𝑅、√𝐵和√𝑅。其拟合拟合能力比线性矩阵更强，提升颜色还原能
力；可以改善 RYYB sensor 的痛点问题，如蓝天偏紫和红色偏橙。
3.3 色彩同步模块
3.3.1 色彩同步原理
色彩同步（colorsync）保持辅路的 AWB 和 CCM 与主路参数一致，降低人
工调参工作量，保持颜色一致性。分为离线标定和在线计算两部分，前者通过
将标准观察者图像与相机测试图像拟合生成转换矩阵 cam2std。后者将主路辅路
统计信息进行预处理、图像匹配、统计相关特征、计算 AWB 补偿值、主路白点
选择最优光源、查找对应光源下 cam2std 的值进行转换，主路辅路计算出
RAW2RAW 值、同步主路的 AWB 和 CCM，最终拆分生成辅路的 AWB 和
CCM。
四、总结
本文档主要针对 Q 平台和 AI 平台关于颜色调试主要模块的原理、方法以及案例进行总结，整理如下：
1. 经过 Q 平台和 AI 平台并行处理后，AI 平台会对不同色温下的场景
进行色度色温优化，使生成图像更加符合人眼色彩感知。
2. 相较于 Q 平台多个模块进行 AWB 调整，AIAWB 相对简单，但其处
理场景效果有限。
3. AI 平台对于 CCM 和 colorsync 可调节的场景较少，目前仍在探索
中。
4. Q 平台对于颜色参数的修改不是很清晰，后期可以尝试将参数修改
量化，形成与色彩变化相关的影响因子。
